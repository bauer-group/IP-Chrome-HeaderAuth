.wxt
.output
node_modules
dist
coverage
.keys
pnpm-lock.yaml
package-lock.json

# Generated by semantic-release on every release, in its own format. It matches
# the format:check glob (*.md), so leaving it in turns every release into a red
# gate on main and on every PR branched from it afterwards. Nobody edits it by
# hand, so there is nothing for prettier to protect here.
CHANGELOG.md
